services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_DB: acme_demo
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5433:5432"
    volumes:
      - acme-postgres:/var/lib/postgresql/data

volumes:
  acme-postgres:
