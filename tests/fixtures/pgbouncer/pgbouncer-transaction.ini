[databases]
* = host=postgres port=5432

[pgbouncer]
listen_addr = 0.0.0.0
listen_port = 6432
auth_type = plain
auth_file = /etc/pgbouncer/userlist.txt
admin_users = postgres
stats_users = postgres
pool_mode = transaction
default_pool_size = 10
min_pool_size = 0
reserve_pool_size = 0
max_client_conn = 100
server_reset_query = DISCARD ALL
ignore_startup_parameters = extra_float_digits
# SQLx uses protocol-level prepared statements for parameterized Postgres
# queries. Transaction-pooling compatibility requires PgBouncer prepared
# statement tracking; PlanetScale-managed PgBouncers default this to 200.
max_prepared_statements = 200
log_connections = 1
log_disconnections = 1
log_pooler_errors = 1
